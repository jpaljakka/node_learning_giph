{
  "name": "node_learning_giph",
  "version": "1.0.0",
  "description": "HBC node practice",
  "main": "\u001b[D\u001b[D\u001b[D\u001bserver.js)",
  "scripts": {
    "build": "tsc && node ./server.js",
    "server": "npm run build && server src",
    "build:server": "nodemon --watch src --exec npm run server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.2",
    "serve": "^11.3.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "node-fetch": "^2.6.0"
  }
}
